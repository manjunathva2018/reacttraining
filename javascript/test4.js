const add=(a,b) => a+b;

const sub = (a,b) => a-b;

const mul = (a,b) => a*b;

const div  = (a,b) => a/b;

// instead of mention export keyword in all the line
// we can create one export object and export all 
export{add, sub, mul, div};